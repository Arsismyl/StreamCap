
services:
  streamcap:
    build: .
    tty: true
    ports:
      - "${PORT:-6008}:${PORT:-6008}"
    environment:
      - PORT=${PORT:-6008}
      - TZ=${TZ:-Asia/Shanghai}
    volumes:
      - ./logs:/app/logs
      - ./config:/app/config
      - ./downloads:/app/downloads
      - ./.env:/app/.env
    healthcheck:
      test: ["CMD", "sh", "-c", "curl -f http://localhost:${PORT:-6008}/about"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - streamcap-network

  video_api:
      build: .
      command: python -m app.api.video_stream_service
      ports:
        - "${VIDEO_API_PORT:-6009}:${VIDEO_API_PORT:-6009}"
      environment:
        - TZ=${TZ:-Asia/Shanghai}
        - PORT=${VIDEO_API_PORT:-6009}
        - CUSTOM_VIDEO_ROOT_DIR=${CUSTOM_VIDEO_ROOT_DIR:-./downloads}
      volumes:
        - ./downloads:/app/downloads
        - ./.env:/app/.env
      depends_on:
        - streamcap
      networks:
        - streamcap-network

networks:
  streamcap-network:
    driver: bridge
